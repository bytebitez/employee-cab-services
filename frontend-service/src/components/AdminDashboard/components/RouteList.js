import React from 'react';

const RouteList = () => {
    return (
        <div>
            <h2>Route Management</h2>
            {/* Add route list and CRUD operations here */}
            {/* Add functionality to pick a list of stops up to 10 */}
        </div>
    );
};

export default RouteList;
