import React from 'react';

const DriverList = () => {
    return (
        <div>
            <h2>Driver Management</h2>
            {/* Add driver list and CRUD operations here */}
        </div>
    );
};

export default DriverList;
